version: 2
 
models:
  - name: mASTER
    description: "This table contains transformed scoring data."
    tests:
      - dbt_utils.equal_rowcount:
          compare_model: source('DBT_Sources', 'MASTER')