version: 2

models:
  - name: DIM_CUSTOMER
    columns:
      - name: customer_key
        tests:
          - not_null

  - name: Dim_Payment
    columns:
      - name: payment_key
        tests:
          - not_null    

  - name: Dim_Product
    columns:
      - name: product_key
        tests:
          - not_null  

  - name: Dim_Store
    columns:
      - name: store_key
        tests:
          - not_null   